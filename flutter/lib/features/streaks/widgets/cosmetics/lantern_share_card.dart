import 'package:flutter/material.dart';

/// E1 (share the composed stage) — filled out in Task D11. Placeholder so the
/// Companion screen's share entry point compiles against its final signature.
Future<void> shareLanternCard({
  required BuildContext context,
  required String skinId,
  required String backdropId,
  required String lanternName,
}) async {}
