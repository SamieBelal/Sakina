<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- CFBundle* keys are intentionally omitted: the target has
	     GENERATE_INFOPLIST_FILE=YES with INFOPLIST_KEY_* build settings, and
	     duplicating those keys here fails the build. Only the widget-specific
	     keys live in this physical plist. -->
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.widgetkit-extension</string>
	</dict>
	<!-- Fonts must be bundled in the extension target — the app's runtime
	     google_fonts are invisible to the widget process. The TTFs are in
	     ios/SakinaWidget/Fonts/ (auto-synced to the target via the
	     file-system-synchronized group). DMSans.ttf is variable; weights come
	     from SwiftUI .fontWeight(). -->
	<key>UIAppFonts</key>
	<array>
		<string>ArefRuqaa-Regular.ttf</string>
		<string>Amiri-Regular.ttf</string>
		<string>DMSerifDisplay-Regular.ttf</string>
		<string>DMSans.ttf</string>
	</array>
</dict>
</plist>
