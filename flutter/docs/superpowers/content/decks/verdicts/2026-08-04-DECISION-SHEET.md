# Decision sheet — 54 decks, blind verification complete

**2026-08-04.** All **11 batches filed**, all **54 decks ruled**. Per-deck evidence lives in the eleven `2026-08-04-VERDICT-*.md` files; each carries a full citation table of every fetch with what the text actually returned.

**This page is the founder's page.** Section 1 is what only you can decide. Section 2 is mechanical and needs no ruling. Section 3 is engineering.

---

## 0 · Headline

| | count |
|---|---|
| **Ship as-is** | **34** |
| **Ship after a mechanical fix** (§2 — no ruling needed) | **8** |
| **Needs a founder ruling** (§1) | **11** |
| **Refuse** | **1** |

**Nothing was found in the 34.** Every citation across them was independently re-fetched and matched.

**Scale of the check:** ~450 āyāt fetched across the wave · every ḥadīth rendered on a beat re-fetched and its printed grade line read · ~20 corpus root sweeps independently re-derived · `name_stories.json` confirmed at 45 decks by direct count in every batch.

---

## 1 · Eleven decisions only you can make

### 1a · The one refusal

**`al-majeed@1` (58) — REFUSE.** Bar 1 fails and no route remains.

The **§9bk ladder is now ruled**: *angelic speech inside divine narration does not carry bar 1*. 11:73 is `قَالُوٓا۟ … إِنَّهُۥ حَمِيدٌ مَّجِيدٌ` — the messengers speaking. The deck's argument (Allah *chose* to narrate it) **proves too much**: Allah narrates a prophet's reported speech too, and that rung is already excluded.

Routes closed on evidence: `م-ج-د` re-confirmed **complete at 4 occurrences** · 50:1 and 85:21 describe the Qurʾān · 85:15 is a bare epithet, already surrendered to id 72 · the unexplored ḥadīth route **was** searched — Tirmidhī 3507 contains `الْمَجِيدُ` but is **`Daʿīf`** and is itself a prophet's reported enumeration.

**Merge question ruled separately: keep 58 and 72 as two decks.** Classical enumerations list them apart. The cost is an honest gap on one Name.

> **This would be the project's fourth refusal and the first not overturned.** Three previous refusals were all overturned, so the prior is against refusing — but this one closed its routes on fetched evidence, which the earlier three did not.

**→ 98 of 99 Names, or reopen it?**

### 1b · Four bar-1 / bar-4 rulings

| deck | the question |
|---|---|
| **`al-muhaymin@1` (18)** | **No text both predicates Allah and demonstrates.** 5:48's `وَمُهَيْمِنًا` modifies **the Book**; 59:23 predicates Allah but inside an eight-epithet chain. Root exhausted at 2 — verified twice, and the verifier **solved the corpus problem**: the code is `?q=hmn`, not `hymn`. No alternate route exists. **Not a redraft — a ruling.** |
| **`ar-rasheed@1` (99)** | **Sharper than anyone framed it.** Its bar-1 carrier 18:17 demonstrates `ه-د-ي` — **Al-Hadi's root, and Al-Hadi is shipped** — not `ر-ش-د`. The deck's own bar-3(c) argues hidāyah and rushd are different claims, so **its carrier contradicts its own separation argument.** Its only rushd text (72:2) is jinn speech via `قُلْ`, the weakest rung. Both Allah-voiced routes confirmed spent. **Possible second refusal.** |
| **`al-ahad@1` (74)** | Bar 4 is a **total** trade with **zero recovery anywhere on the deck** — the shared duʿā's verb `وَحِّدْ` is *Al-Wahid's* root. Its siblings all recover their root somewhere. The deck says "traded" without saying "totally". Understated, not wrong. |
| **`malik-ul-mulk@1` (88)** | Bar 4 is **more traded than claimed**: 64:1's `ٱلْمُلْكُ` is the verbal noun *dominion*, not the participle `مَالِك` *owner* that opens the compound Name. **A one-time ledger ruling is wanted**, not per-deck relitigation: *does a cognate verbal noun count as root evidence for bar 4?* |

### 1c · Four register calls — read these as the 11pm reader

| deck | what is on screen |
|---|---|
| **`al-qahhar@1` (22)** | **Sūrat al-Fīl rendered whole** — an army destroyed, *"like eaten straw."* **Another agent on this project independently refused this exact passage on bar 5 for a different Name.** Verifier ruled CONTESTED, not a clean pass. |
| **`al-muzill@1` (44)** | The **rendered excerpt itself** is a rebuke — 7:152's *"anger from their Lord and humiliation."* Not a neighbour. Resolved toward shipping, flagged as **the one row to read personally**. |
| **`al-mumeet@1` (70)** | Passes the literal bars, but read as a grieving reader: the angel-striking image, and a *"declined years of life"* framing that could read as prescriptive to someone fighting to live. **CONTESTED, not pass or fail.** |
| **`al-akhir@1` (80)** | Beat 4's authored prose says **"A man comes out of the Fire crawling."** Bar 5 bans Fire adjacency *read strictly*. **This overturns my own R2 "upheld"** — my argument answered a narrower question than the bar asks. **Fix available that touches no scripture** (§2), so this is only a ruling if you want to keep the word. |

### 1d · Two catalogue-locked duʿā defects

**`al-hakeem@1` (26) + `al-khabeer@1` (49) — HOLD.** Their shared locked duʿā invokes **`يَا لَطِيفُ` — Al-Lateef, a third and already-shipped Name.** Neither deck's own Name appears anywhere in its duʿā. The verifier checked every shipped deck and **found no other mismatch of this severity**, and split it from `ash-shaheed@1` (60), whose duʿā at least uses **its own root and verb** even though the vocative is Al-Baseer's.

**I had lumped all three as "disclosed, ship". That was wrong** — 26 and 49 are a different thing from 60. **→ catalogue fix, or explicit sign-off?**

### 1e · One contested engine

**`al-ghaniyy@1` (92) — register CONTESTED.** 35:16's *"if He wills, He can do away with you and bring forth a new creation"* is starker than "swept, clean" implies, and **the softening lives in the deck's authored bridge and takeaway, not in the verse.** Wouldn't block alone; wouldn't stamp clean either.

---

## 2 · Mechanical fixes — no ruling needed

**Two already applied by the coordinator:**

- ✅ **`al-mutakabbir@1` (19)** — beat 5's 45:37 was an **unattributed re-rendering** matching none of six translations (*"all greatness in"* vs Saheeh's *"[all] grandeur within"*). Now Saheeh verbatim. *"Grandeur"* also separates from `al-kabeer@1`'s field, *The Greatest*.
- ✅ Earlier R2 fixes **independently re-verified as correct** by the blind pass: `al-mateen@1`'s 50:38 and `al-qawiyy@1`'s 30:54 are genuinely verbatim Usmani, not spliced; `al-wajid@1`'s duʿā is byte-equal to the catalogue with no dagger alif; `al-khabeer@1`'s interior-ellipsis fix took.

**Eight outstanding, each with the fix named:**

| deck | fix |
|---|---|
| **`al-musawwir@1` (21)** | **Re-run bar 5 from live fetches.** Its successor-sweep table is **5 of 6 wrong** (§4). Rendered beats and root sweep are confirmed solid and need no rework |
| **`al-barr@1` (85)** | Beat 6 → **Abdel Haleem verbatim**: *"We used to pray to Him: He is the Good…"* One named translation, ellipsis, keeps *"the Good"*. Zero cost |
| **`al-hakam@1` (47)** | Carrier 22:56 → **22:69** (`ٱللَّهُ يَحْكُمُ بَيْنَكُمْ`). Clean both sides; **second person** puts the reader inside the sentence, which is this deck's engine. If 16:124 is used instead, 16:125's `بِٱلْحِكْمَةِ` is id 26's and must not render |
| **`al-akhir@1` (80)** | Drop the literal word *"Fire"* from beat 4's **authored paraphrase** — not a quotation, so the Sources table is untouched |
| **`al-wali@1` (83)** | Mis-sourced: the 13:11 rejection lives in the **unshipped** `al-waliyy` draft, not shipped `al-waliyy@1` |
| **`al-mubdi@1` (67)** | False attribution: 32:7 is **not** cited in shipped `al-khaliq@1` |
| **`al-ghaniyy@1` (92)** | *"harm"* count is **n=2, not n=1** (missed `al-haleem@1`'s *"harmful"*); and its Sūrat Fāṭir precedent cites `al-wahid@1` for the **opposite** of what that draft now says |
| **`al-adl@1` (48)**, **`al-azeem@1` (50)**, **`al-qawiyy@1` (62)**, **`al-muqtadir@1` (76)**, **`al-baith@1` (59)** | Disclosure gaps: 82:7 undisclosed · *"same sentence"* overstated · Bukhārī 6610's book heading is `كتاب القدر`, not `كتاب الأدب` · 54:53 never fetched · 22:6 never fetched |

**Two self-measurement errors** — real, judged non-blocking: `al-muqaddim@1`/`al-muakhkhir@1` claim "zero hits at n≥4"; the true max is **5** (*"you are the one who"*). And `al-qawiyy@1`/`al-mateen@1`'s takeaways **describe each other in swappable language** — *"not a supply you could run out of"* vs *"never depleted by being leaned on"* — with shipped `al-kareem@1` independently using the same *"supply"* metaphor. Neither deck searched the word.

**Three duʿā pins ruled YES**, ready for `renderedDuaSources`:

- `al-jami@1` → `Qur'an 3:9` — rasm-identical, complete āyah, **no qualifier needed**
- `al-awwal@1`/`al-akhir@1` and `az-zahir@1`/`al-batin@1` → `Sahih Muslim 2713a (excerpt)` — both couplets fetched in full and confirmed genuine excerpts
- `an-nafi@1` → `Sunan Ibn Majah 925` — the *"rasm-identical but not byte-identical"* worry was traced to the gate's actual assertion and **does not affect what the gate compares**

---

## 3 · Engineering — the one that can hurt a user tonight

**Nothing stops `ad-darr@1` — *The Distresser* — shipping without `an-nafi@1`.** Confirmed by three verifiers reading the gate directly: both pair assertions loop over `chip_keys`, and every pair deck carries `[]`.

Ad-Darr's own analysis says beat 8 **"structurally cannot resolve into relief"** without its partner — the takeaway hands off to An-Nafi by name. **A reader in distress can draw the setup with no payoff.**

**And populating `chip_keys` does not fix it — it breaks the gate.** `chipKeys` is a fixed 7-item *mood-tag* set; none of the pair decks declares `position_in_pair` 1 or 2, and none carries a synergy beat. Naive population introduces a **new hard failure** on *"maps to exactly two decks, positions 1 and 2"*.

**These five pairs need their own mechanism.** Aḍ-Ḍārr/An-Nāfiʿ · Al-Qābiḍ/Al-Bāsiṭ · Al-Khāfiḍ/Ar-Rāfiʿ · Al-Muʿizz/Al-Muẓill · Al-Muqaddim/Al-Muakhkhir.

**Interim option if you want Ad-Darr sooner:** soften beat 8's forward reference so the deck resolves standalone.

---

## 4 · The wave's most important finding

**`al-musawwir@1`'s bar-5 successor sweep is 5 of 6 fabricated or mislabeled.**

| cited as | the quoted text actually is |
|---|---|
| 3:5 | **31:28** |
| 40:63 | **no āyah** — the "cattle" simile does not exist |
| 82:6 | 82:6's opening spliced onto **82:7** |
| 82:9 | **50:45** |
| 40:65 | **23:116** |

**It survived R0, R1, a blind Sonnet verifier, and R2** for one reason: every pass scoped itself to what *renders*. **Nobody fetched the citations that exist only to justify a bar-5 verdict** — the section everyone had a principled reason to skip. The deck then reasoned at length about a "cattle simile" that isn't there.

**And the blind verifier marked 40:65 ✅ as *"matches exactly."*** It doesn't. A verification error inside the verification — which is the honest lesson: **on this surface a single independent check was not enough.**

**Coordinator sweep, all 54 drafts:** every off-beat `<āyah>: "<English quotation>"` claim (21 project-wide) and every table-format beat (25) fetched and diffed. **The fabrication is confined to this one deck**, plus the one table-format defect already fixed. Three benign variants elsewhere (`al-bari` 57:24 bracket, `al-jami` 3:8 word order, 3:10 *"their children"*).

**Limit, stated:** most drafts write successor sweeps as **Arabic plus an English gloss**, which neither pattern reaches. **That surface is still unaudited across all 54 decks.**

---

## 5 · Five corrections to `2026-08-04-R2-VERIFICATION.md`, all accepted

1. **Declined to rule on the §9bk ladder** and left `al-majeed@1` stamped `VERIFIED`. Premature for id 58.
2. **Framed the judgment-four ruling as "reject all four or none."** That conflates two axes — a verifier can accept the group ruling and still send one deck back for an orthogonal reason, which is what happened.
3. **Flattened three duʿā defects into one category.** 26/49 (an unrelated Name) are not 60 (own root, wrong vocative).
4. **Marked Bukhārī/Muslim entries "Ṣaḥīḥ ✅" with no printed grade line.** Correct scholarship, but collection-level *inference* rendered identically to Tirmidhī 3563's read line `Hasan (Darussalam)`. **What was read and what was inferred must look different.**
5. **Parsed only `>` blockquote beats**, so twelve table-format drafts had their beats unchecked. That gap cost exactly one miss (45:37), now closed and the whole surface swept.

---

## 6 · The reflection beat is not a per-deck blocker

**0 of 45 shipped decks have one.** 44 have exactly 8 beats; one has 10. So the 19 drafts without a `reflection` are not defective — **the beat is new, 35 drafts adopted it early, and the entire shipped corpus predates it.**

**This is an asset-scope decision, not a ship blocker:** backfill 64 decks, or leave the slot to the runtime and let the 35 stand ahead. My R2 called this a completion gap in 19 drafts; that framing was wrong.

---

# ADDENDUM — 2026-08-04, after the fix pass

**Everything mechanical is done.** Below: what was fixed, what the transcription pass caught, and the decisions that remain.

## A · Fixes applied (14 drafts)

`al-barr@1` beat 6 → **Abdel Haleem verbatim**, *"We used to pray to Him: He is the Good…"* · `al-mutakabbir@1` beat 5 → **Saheeh verbatim** (45:37 was an unattributed re-rendering) · `al-mubdi@1` — 32:7 is **not** cited in shipped `al-khaliq@1`; free ground · `al-wali@1` — the 13:11 rejection is in the **draft**, not the shipped deck (whose verse beat is 93:6) · `al-muqtadir@1` +54:53 and `al-baith@1` +22:6, both fetched, both clean · `al-adl@1` — grammar corrected and **82:7 disclosed** · `al-qawiyy@1` — Bukhārī 6610's heading is **`كتاب القدر`**, not `كتاب الأدب` · `al-azeem@1` — *"the same sentence"* → the same **act of tasbīḥ** · `al-ghaniyy@1` — *"harm"* is **not a hapax** (3 occurrences in 2 decks; `al-haleem@1`'s *"harmful"* was missed), and its Fāṭir precedent cited a sibling that now says the opposite · `al-muqaddim@1`/`al-muakhkhir@1` — twin-diff is **5**, not 0 · `al-musawwir@1` — **bar 5 re-run from scratch** (below).

**Two swappable takeaways rewritten.** `al-qawiyy@1` called Al-Mateen *"not a supply you could run out of"*; `al-mateen@1` called Al-Qawiyy *"never depleted by being leaned on"* — the same claim, and shipped `al-kareem@1` already uses *"a supply that runs down"*. **Twin-diff on beat 8 is now 2, and neither word survives.**

**And a logic error neither verifier caught: both decks named the *other* as "the second Name of your answer."** They cannot both be second. `al-mateen@1` is `position_in_pair: 2`, so its forward reference **and its pair-synergy label** are gone — which is also what the gate requires.

## B · `al-musawwir@1`'s bar 5, re-run — the fabrication hid a real hit

Two of three neighbourhoods came back **cleaner** than R0 claimed. The third came back **worse**:

**The true 82:9 is `بَلْ تُكَذِّبُونَ بِٱلدِّينِ` — "No! But you deny the Recompense."** Second person plural. **A direct accusation of the reader, one āyah after the deck's last story beat** — and the fabricated substitute R0 printed in its place (*"you are not over them a compeller"*, actually 50:45) was **milder than the text it replaced.** The error ran in the direction that made the deck pass.

**Ruled PASS, on the project's own precedent:** 82:9 is not rendered, and `al-hakeem@1` (n−1 is a Fire āyah) and `al-khabeer@1` (n−1 is a visceral rebuke) both passed the same shape, disclosed. **It is the sharpest instance in the wave** — 82:8 is the deck's *last* story beat — and it is now disclosed at full strength rather than smoothed.

## C · Transcription — 54/54 decks, 467 beats, zero validation failures

`verdicts/transcribe.py` + `verdicts/2026-08-04-STAGED-54-DECKS.json`. Validated against every ship-gate assertion that does not require founder sign-off. **Coverage 99/99, no duplicate ids, no overlap with the shipped 45.**

**Six defects the transcription pass caught that no review had:**

| | |
|---|---|
| **Three decks would have shipped under the wrong Name** | `al-muizz@1`→8, `al-muzill@1`→8, `al-qahhar@1`→9 — the parser took `min()` over every id in the file, and these drafts discuss siblings by id. Now read from the deck's own metadata block, with a slug/Name cross-check that refuses on mismatch |
| **60 story beats leave a quotation open across beats** | **0 of 45 shipped decks do this.** On screen each beat is its own card, so beat 3 shows an opening quote that never closes. Now balanced at the ellipsis, shipped-style |
| **`al-batin@1`'s reflection would have rendered its own sweep notes** | *"Swept, 45 decks: max shared word-run 3…"* — to the user |
| **`al-ahad@1`/`al-wahid@1` story beats carried `*(source: Qur'an 6:101)` inline** in body text | now moved to `source` |
| **`al-musawwir@1`'s verse beat carried `*(R1: truncated at the clause boundary — see bar 3(b))*`** | stripped |
| **`al-muhyi@1`'s citation read *"(paraphrase throughout — not a verbatim quotation; trailing qadir clause dropped)"*** | a note to a verifier, rendered to a reader. Now `(paraphrased)` |

**None of these was a scripture error.** They are the class of defect that only appears when you build the thing that ships — which is the argument for building it before sign-off rather than after.

## D · What is left, and it is all yours

**Nothing mechanical remains.** Every deck is fixed, staged, and validated. The eleven decisions in §1 stand unchanged, and they reduce to **five questions**:

1. **Bar 5's scope** — does *"no Fire/Judgment adjacency"* mean *none on screen*, or *the reader is never placed in it*? Settles `al-akhir`, `al-qahhar`, `al-muzill`, `al-mumeet` at once. Four verifiers each drew this line differently, which says the rule is underspecified rather than the decks are.
2. **Bar 4's root standard** — does a cognate verbal noun count? Settles `malik-ul-mulk`, `al-ahad`, and every future deck.
3. **`al-majeed@1`** — accept the refusal (98 Names) or reopen it.
4. **`ar-rasheed@1`** — its bar-1 carrier demonstrates **Al-Hadi's** root. Likely a second refusal. And **`al-muhaymin@1`**, where no text both predicates Allah and demonstrates.
5. **The Al-Lateef duʿā** on `al-hakeem@1`/`al-khabeer@1` — catalogue fix, or ship with sign-off.

Plus **`al-hakam@1`'s carrier**, which is a real trade with no dominant option: 22:56 has punishment on both sides but continues into *"the Gardens of Pleasure"*; **22:69 is clean on both sides and second-person (`بَيْنَكُمْ`)** but is a bare declarative. **My recommendation is 22:69** — this deck's engine is *the reader with a case nobody will hear*, and second person puts them inside the sentence. Say the word and I'll cut it.

**And one engineering item that is not a deck question:** nothing stops `ad-darr@1` shipping without `an-nafi@1`, and populating `chip_keys` breaks the gate rather than fixing it.
