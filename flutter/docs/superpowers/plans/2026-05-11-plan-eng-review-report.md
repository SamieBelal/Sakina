# Plan Eng Review Report — 2026-05-11 content gap plans

Generated by `/plan-eng-review` on 2026-05-11. Branch: master. Repo: SamieBelal/Sakina.

**Plans reviewed (6):**
- `2026-05-11-plan-0-allahnames-and-eval-foundation.md` (added by review)
- `2026-05-11-reflection-verse-catalog-expansion.md`
- `2026-05-11-browse-duas-expansion.md`
- `2026-05-11-discovery-quiz-expansion.md`
- `2026-05-11-name-anchors-backfill.md`
- `2026-05-11-name-teaching-expansion.md`

## Decisions captured

| # | Issue | Resolution |
|---|---|---|
| 1 | AI prompt size explosion if Plan 1 ships as written (~10KB added per reflect call across 99 verses) | Plan 1 Task 0 added: drop `buildApprovedVersePrompt` from system prompt. Catalog becomes deterministic source. |
| 2 | `allahNames` Dart const has 16/99 entries; `findCanonicalName` rejects 83 Names; silent dependency for Plans 1, 2, 5 | New Plan 0 added: backfill to 99 + create eval foundation. |
| 3 | No LLM eval to detect Name-pick quality regressions from prompt changes | Plan 0 establishes 25-row fixture + baseline. Plans 1, 2, 5 each run it as a regression gate. |
| 4 | Plan 4's batched rollout creates intermediate UI states (some Names anchored, some not) | Plan 4 reworked: author in 7 working batches, ship as one squashed commit. |

## Critical gap closed mid-review

- Plan 1 missing an unknown-name fallback in `normalizeApprovedVerses` — would have produced verseless cards for any AI hallucination. Added Task 3.5 + test pinning the safety net.

## Required outputs

### NOT in scope

- Translating any new content to Arabic, Urdu, Malay, Turkish, French (filed as TODO: i18n).
- Consolidating the 3 dua storage formats (Dart const verses vs Dart const NameTeaching vs JSON browse_duas) — filed as TODO.
- Fixing the AI's choice of source attribution for prophetic stories beyond the Sahih grades captured in ledgers (no canonical grading authority defined).
- Per-language eval fixtures (only English fixture in Plan 0).
- Live A/B testing the eval baseline against pre-Plan-1 prompt vs post — baseline pinned once and held; manual review of failing rows on each plan.

### What already exists (reused, not rebuilt)

- `normalizeApprovedVerses` fallback in `reflection_verse_catalog.dart:122` — Plan 1 leans on this; we extend it but don't replace.
- `_searchLocalDuas` keyword + emotion-tag scoring in `ai_service.dart:807` — Plan 2 extends `_semanticMap` and the JSON; ranking logic untouched.
- `getRelevantTeachings` keyword matcher in `knowledge_base.dart` — Plan 5 extends the corpus; matcher untouched.
- `validate_names.dart` `findCanonicalName` — Plan 0 fixes by extending the canonical source, doesn't change the validator.
- `public_catalog_service` asset-bundle loader — Plans 2, 3, 4 ship JSON edits that flow through this unchanged.

### TODOS captured (in `TODO.md`)

- Consolidate the 3 Islamic-content storage formats — added.
- i18n for new content (Arabic, Urdu, Malay, Turkish, French) — added.

### Failure modes (summary)

| Plan | Top failure mode | Mitigated by |
|---|---|---|
| 0 | Apostrophe-escape errors in copied Dart strings | `flutter analyze` catches at compile time |
| 1 | AI returns non-canonical Name → catalog miss → verseless card | Task 3.5 safety net + Plan 0 canonical fix |
| 2 | New `_semanticMap` keyword shadows existing routing | Plan 2 searchability tests per new category (no regression test on existing; minor flag) |
| 3 | Score key slug format mismatch | Plan 3 Task 1 inspects contract before authoring |
| 4 | Intermediate broken UI during batched rollout | **Resolved** — single squashed commit |
| 5 | Over-broad emotionalContext matches everything | Caught by Plan 0 eval (regression gate) |

### Worktree parallelization

- **Lane A (sequential):** Plan 0 → Plan 1
- **Lane B (sequential):** Plan 0 → Plan 2 (conflict with Lane A on `ai_service.dart`; rebase between)
- **Lane C (sequential):** Plan 0 → Plan 5
- **Lane D (parallel start):** Plan 3
- **Lane E (parallel start):** Plan 4

Land Plan 0 first. Then A, B, C in sequence (or A and B both touch `ai_service.dart` — merge one, rebase the other). D and E are fully independent.

## Completion summary

- Step 0: Scope Challenge — scope reduced (added Plan 0 foundation, reshaped Plan 4 to squashed-commit)
- Architecture Review: 2 issues found, both resolved (prompt explosion, allahNames gap)
- Code Quality Review: 0 decisions required (2 minor flags auto-resolved)
- Test Review: diagram produced, 6 gaps identified, 5 addressed inline (eval + unknown-name fallback + squashed rollout + canonical backfill + new Plan 1 prompt test). Remaining gap: Plan 2 missing regression test on existing keywords (informational).
- Performance Review: 1 issue, resolved via Issue 1 decision
- NOT in scope: written
- What already exists: written
- TODOS.md updates: 2 items proposed and accepted
- Failure modes: 1 critical gap flagged and closed (unknown-name safety)
- Outside voice: skipped by user
- Parallelization: 5 lanes, 2 fully independent, 3 chained on Plan 0
- Lake Score: 4/4 decisions chose the complete option

## Verdict

**CLEARED for implementation** — all 6 plans are ready to execute. Recommend landing Plan 0 first as solo work, then opening Plans 3 and 4 PRs in parallel while Plans 1, 2, 5 chain on Plan 0's merge.

---

## Per-plan eng review pass (2026-05-11, second wave)

After the cross-plan review above, 6 parallel subagents ran a focused per-plan engineering review. Findings docs at `2026-05-11-plan-N-eng-review-findings.md`. Key per-plan blockers caught and patched into the plans:

| Plan | Findings | Patched |
|---|---|---|
| 0 | 2 P0 + 2 P1: eval runner missing `env.json`, demo-fallback poisons baseline, id=1 "Allah" collision, `Ash-` prefix regex collision | ✅ env.json flag, demo-fallback hard-fail, skip id=1 Allah, structured rationale schema |
| 1 | 1 P1 + 6 minor: 5 entries use `Quran N:N` semantic violation, missing allahNames↔JSON cross-check, batch math ambiguity, ledger reviewer gate | ✅ Task 0.5 renames the 5 entries + tightens regex; cross-check test added; ledger completeness test added; debug warning on unknown-name fallback; bundle id fixed |
| 2 | 1 critical + 2 high: no regression test on existing 15 categories, `sick`/`temptation` overlaps steal hits, Task 9 reflect eval is theatre | ✅ regression test pinning existing 15 categories' top-hit category; `sick` no longer co-tags protection; `tired` co-tagged with anxiety; `failing` dropped; Task 9 replaced with dedicated `findDuas` smoke eval |
| 3 | aggregator located (`calculateQuizResults` in `discovery_quiz.dart:510`), example slugs wrong (`al-lateef` ≠ `al-latif`), baseline was 32 not 10-15 | ✅ placeholders replaced with real function name, example slugs canonicalized, baseline updated to 32, target raised to 55, regex simplified, fresh-install pre-step removed (quiz is post-onboarding), dual-source warning added |
| 4 | 2 P0: slug strategy broken (transliteration ≠ anchor keys, orphans 14 of 32), `expectedCount=32` hardcoded | ✅ `_transliterationToAnchorSlug` translation map added; non-canonical anchor set; `expectedCount` bump task added; em-dash punctuation rule; two-reviewer flow (ED + TH) |
| 5 | 2 critical blockers: lowercase test fails against 47 existing strings, compound-key parts use non-canonical spellings | ✅ Task Step 0 added (lowercase pre-pass); coverage test normalizes through `findCanonicalName`; `DuaContent` → `NameTeachingDua` with named args; probe assertion tightened to top-1; ledger split into `Story Type` + `Grade` |

**Patterns surfaced across plans:**
1. **Transliteration drift** is the codebase's #1 silent killer. Names exist under at least 3 parallel spelling systems (`collectible_names.json` uses `Al-Lateef`, anchor JSON uses `al-latif`, knowledge_base.dart uses `Al-Latif`). Every coverage-test plan needs either a normalization layer or an explicit translation map. Codified the layered approach in Plan 4's translation map and Plan 5's `findCanonicalName` route.
2. **`public_catalog_contracts.dart` hardcoded `expectedCount`** is a cross-plan landmine. Plan 4 now bumps it. Plans 2 and 3 don't have count contracts today; if they grow, check `public_catalog_contracts.dart` in advance.
3. **The reflect eval doesn't cover `_semanticMap` or `findDuas`** — wrong-tool-for-the-job for Plans 2 and 3 originally. Plan 2 now has a dedicated `findDuas` smoke eval; Plan 5 keeps the reflect eval (correct surface).
4. **Bundle id is `com.sakina.app.sakina`** (verified by 6 separate agents). All simulator steps updated.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAN (PLAN) | 4 issues, 0 critical gaps |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

**UNRESOLVED:** 0
**VERDICT:** ENG CLEARED — ready to implement
