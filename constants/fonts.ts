// Using system serif for Arabic — iOS has excellent built-in Arabic rendering.
// Amiri can be added via EAS native build when ready.
export const Fonts = {
  arabic: 'Georgia',
  arabicBold: 'Georgia',
  body: undefined,
} as const;
